# preset/templates

工作区模板源（不作为技能注册）。M2 将写入：

- `AGENTS.md` 导师人格模板
- `workspace/` 工作区目录骨架与各目录 README
- 五类记忆、契约、干预记录模板
- `views/` 模板

安装后 `baseUrl` 是唯一可靠的定位锚：引导技能经自身资源根反推预设根，
模板在 `<preset>/templates/`。
