# views/indexes/ — 检索索引

按需生成、可删除重建的检索入口，建议维度：
`by-capability.md`（能力维度）、`by-project.md`（项目）、`by-topic.md`（主题）、
`by-time.md`（时间）、`by-relation.md`（supersedes/superseded_by 关系）。

索引只列相对路径与一句话摘要，不复制记录正文。
