# views/ — 非权威派生视图

`active-context.md` 是导师默认上下文入口（有预算）；`challenged.md` / `archived.md`
是检索视图；`indexes/` 存多维索引。全部可由契约与记录重建：删除后重新生成，
**不要手改视图后反向覆盖原始记录**。

由 `learner-model-calibration` 技能在维护长期状态后重新生成。
